{
  "name": "vidasaludable-webhook",
  "version": "1.0.0",
  "description": "GitHub webhook deploy server for vidasaludable",
  "main": "webhook.js",
  "scripts": {
    "start": "node webhook.js"
  },
  "engines": {
    "node": ">=18"
  }
}
